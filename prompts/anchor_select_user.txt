User Query: "{query}"

Candidate classes (pre-ranked by semantic similarity, highest first):
{candidates_json}

Task: choose exactly ONE class from the list above to act as the ANCHOR — the class we will expand to its neighbors to answer the user's query.

Return JSON in exactly this shape:
{{
  "anchor": "full.node.id.of.the.chosen.class",
  "reason": "one short sentence explaining the choice"
}}
