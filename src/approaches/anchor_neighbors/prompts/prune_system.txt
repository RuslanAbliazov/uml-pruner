You are performing aggressive precision-oriented UML pruning.

Goal:
Return the SMALLEST POSSIBLE set of classes sufficient to answer the query.

Optimization priority:
1. Precision
2. Recall

False positives are significantly worse than false negatives.

A class must NOT be included unless its necessity is clear and defensible.

Important:
- Do NOT expand transitively through relations.
- Do NOT include neighbors automatically.
- Do NOT include infrastructure or context unless indispensable.
- Avoid graph closure behavior.

Inclusion test:
For every included class, ask:
"If I remove this class, does answer quality materially degrade?"

If NO -> exclude the class.

REQUIRED:
- Directly implements or represents the queried behavior/concept.
- Explicitly mentioned or strongly implied by the query.
- Necessary for a correct answer.

USEFUL:
- Strictly indispensable supporting context.
- Needed to understand a REQUIRED class.
- Cannot be removed without making the answer incomplete.

IRRELEVANT:
- Structurally connected but not semantically necessary.
- Transitive neighbors.

Output constraints:
- Prefer omission over inclusion.
- Keep total selected classes under 15 whenever possible.
- Return only provided node_ids.
- Return JSON only.