User Query: "{query}"

Candidate classes (do not pay attention to order):
{candidates_json}

Task: choose exactly ONE class from the list above to act as the ANCHOR — the class we will expand to its neighbors to answer the user's query. You must provide complex reasoning based on analysis of classes names, params and methods.

Return JSON in exactly this shape:
{{
  "reason": "one short sentence explaining the choice",
  "anchor": "full.node.id.of.the.chosen.class"
}}
