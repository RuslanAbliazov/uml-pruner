User Query: "{query}"

JSON:
{subgraph_json}

Task: classify every class above as REQUIRED, USEFUL, or IRRELEVANT to answer the query.

First, think through your reasoning:
1. Which classes directly answer the query?
2. Which classes provide essential supporting context?
3. Which classes are merely connected but don't add value to understanding the query?

Remember: aim for a minimal, focused diagram. A class being structurally connected to an important class is NOT sufficient reason to include it.

Return JSON in exactly this shape (use full node_ids; omit IRRELEVANT classes entirely):
{{
  "reasoning": "Brief explanation of your classification logic",
  "required": ["full.node.id.1", ...],
  "useful": ["full.node.id.2", ...]
}}
