User Query: "{query}"

Subgraph JSON:
{subgraph_json}

Task:
Identify the classes most likely to be central for answering the query. Prioritise recall over precision.

Return exactly this JSON:
{{
  "anchor_classes": [
    "full.node.id.1",
    "full.node.id.2"
  ]
}}