User Query: "{query}"

Anchor classes:
{anchor_classes}

Subgraph JSON:
{subgraph_json}

Task:
Apply the minimal sufficient diagram principle. Classify each class as REQUIRED, USEFUL, or IRRELEVANT and output only the kept (REQUIRED + USEFUL) classes.

Return exactly this JSON:
{{
  "required": [
    "full.node.id.1"
  ],
  "useful": [
    "full.node.id.2"
  ]
}}