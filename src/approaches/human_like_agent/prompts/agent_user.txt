USER QUERY: {query}

ANCHOR CLASSES (pick the BEST one based on query terms): {anchors}

Output format:

```json
{{
    "required": [org.node.id.a, org.node.id.c],
    "useful": [org.node.id.b, org.node.id.d]
}}
```

BEGIN!
