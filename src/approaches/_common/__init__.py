"""Helpers shared across approach implementations.

Approach-specific runners live in sibling packages (``rag_classes_filter``,
``anchor_neighbors``, ...). Anything they all need (subgraph filtering,
package grouping, etc.) lands here.
"""
